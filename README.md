# AI Face Mesh Tracker (RTX 5060 Optimized)

A modular Python application that tracks 468 3D facial landmarks in real-time.

## 🚀 Features
- **High Performance:** Optimized for NVIDIA RTX 50-series GPUs.
- **Modular Design:** Face detection logic is separated into `face_module.py` for easy reuse.
- **3D Landmarks:** Real-time tracking of 468 points including eyes, lips, and face contours.

## 🛠️ Installation
1. Clone the repo: `git clone https://github.com/YOUR_USERNAME/face_mesh.git`
2. Install dependencies: `pip install -r requirements.txt`
3. Run the app: `python main.py`
